/**
 * VaultTransaction — intermediate abstract class for vault transactions.
 */
import { Transaction } from '../transaction.js';

export abstract class VaultTransaction extends Transaction {
  // Shared vault logic can be added here.
}
