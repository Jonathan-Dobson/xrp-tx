/**
 * CredentialTransaction — intermediate abstract class for credential transactions.
 */
import { Transaction } from '../transaction.js';

export abstract class CredentialTransaction extends Transaction {
  // Shared credential lifecycle logic can be added here.
}
