/**
 * OracleTransaction — intermediate abstract class for oracle transactions.
 */
import { Transaction } from '../transaction.js';

export abstract class OracleTransaction extends Transaction {
  // Shared oracle logic can be added here.
}
